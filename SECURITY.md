# 安全政策

VPS 管理与安全平台会以管理员权限管理远程登录、防火墙和系统服务。安全问题应优先通过非公开方式处理。

## 支持范围

安全修复优先覆盖：

- 当前最新 2.x 预发布或正式版本；
- 仍在更新通道中的上一 2.x 版本；
- 1.x 迁移入口本身。

`legacy/` 中保存的历史实现只用于追溯，不再接收功能更新。第三方诊断工具及其下游组件受各自上游安全政策约束，但平台维护的固定入口、摘要校验和提示属于本项目责任范围。

## 报告安全问题

优先使用 GitHub 仓库 Security 页面中的私密漏洞报告功能（如果该功能可用）。如果暂时没有私密入口，请不要在公开 Issue 中粘贴漏洞利用细节、服务器地址、凭据或日志；可以先提交一条不含敏感信息的请求，说明需要建立私密沟通渠道。

报告建议包含：

- 受影响版本和系统；
- 问题类型和影响；
- 最小复现步骤；
- 是否可能导致 SSH 失联、权限提升、配置覆盖或供应链风险；
- 已做过的安全验证；
- 建议修复方式（如有）。

## 不要提交的内容

- VPS IP、root 密码、API Token、Cookie；
- SSH 私钥、完整 `authorized_keys`；
- 未脱敏的认证日志和用户数据；
- 可直接攻击仍在线系统的完整利用代码。

## 处理原则

- 先确认影响和安全停止方式，再开发修复。
- 修复必须包含回归测试。
- 高风险修复需要真实 VPS 验证和恢复方案。
- 在修复可用前不公开能够扩大攻击面的细节。
- 发布修复后更新 Changelog、安全说明和受影响版本范围。

本政策不承诺固定响应时限；项目会按影响范围和可利用性优先处理可能导致访问丢失、权限提升、远程代码执行或供应链破坏的问题。
