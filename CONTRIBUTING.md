# 贡献指南

感谢参与 VPS 管理与安全平台。项目欢迎错误报告、文档改进、测试结果和经过讨论的功能实现。

## 开始之前

- 阅读 [`README.md`](README.md) 和 [`ARCHITECTURE.md`](ARCHITECTURE.md)。
- 安全相关改动还应阅读 [`COMPATIBILITY.md`](COMPATIBILITY.md) 和 [`MODULE_SPEC.md`](MODULE_SPEC.md)。
- 较大功能先建立 Issue，确认范围和安全边界后再实现。
- 安全漏洞不要提交公开 Issue，请遵循 [`SECURITY.md`](SECURITY.md)。

## 开发原则

1. 保留用户访问优先于强化配置。
2. 不假设 SSH 使用端口 22。
3. 不覆盖用户拥有的主配置文件。
4. 系统修改遵循 `check -> plan -> apply -> verify`。
5. 高风险修改应支持备份和回滚。
6. 不使用未经固定和校验的远程 root 执行入口。
7. 普通菜单和命令模式调用同一模块实现。
8. 新增行为必须包含相称的自动化测试和文档。

## 本地验证

```bash
./tests/run.sh
./scripts/build-release.sh
```

提交前还应确认：

```bash
git diff --check
```

真实 VPS 测试方法见 [`docs/TESTING.md`](docs/TESTING.md)。不要在生产服务器上首次验证高风险修改。

## Pull Request

- 一个 PR 聚焦一个明确目标。
- 说明用户问题、变更内容、安全影响、测试结果和未覆盖场景。
- 不混入无关格式化或用户本地文件。
- 更新受影响的 README、兼容性、测试记录或 Changelog。
- CI 通过不等于高风险实机验证完成；需要时附上对应验证记录。

## Shell 风格

- 以 Bash 为目标，并保持现有支持环境兼容。
- 使用 `set -u` 时为可选变量提供安全默认值。
- 引用变量和路径，避免不受控的分词与通配。
- 使用临时目录、明确权限和清理陷阱。
- 错误信息应说明用户能采取的下一步，而不只是输出底层失败。

## 文档变更

文档是产品的一部分。命令、菜单、路径、版本和兼容性声明必须与实际代码一致。公开文档描述已经实现和验证的行为，不公开维护者的私人产品计划。
